spring:
  application:
    name: GP_solutions_task

  datasource:
    url: jdbc:h2:mem:gp_solutions_db
    username: username
    password: password
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8092